const targetingEngine = require('../../../../src/services/targeting/targetingEngine');
describe('targetingEngine', () => {
  it('should be defined', () => { expect(targetingEngine).toBeDefined(); });
});
