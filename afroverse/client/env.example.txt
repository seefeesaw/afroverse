# API Configuration
VITE_API_URL=http://localhost:5000/api

# Mock Mode - Set to 'true' to use dummy data without server
VITE_USE_MOCK_DATA=true

# Socket Configuration
VITE_SOCKET_URL=http://localhost:5000

# App Configuration
VITE_APP_NAME=Afroverse
VITE_APP_VERSION=1.0.0

# Instructions:
# 1. Copy this file and rename to .env.local
# 2. Set VITE_USE_MOCK_DATA=true to enable mock mode (no server needed)
# 3. Set VITE_USE_MOCK_DATA=false when you want to connect to real server

